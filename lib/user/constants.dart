/// Legacy export so existing imports keep working.
///
/// Actual implementation lives in `lib/common/constants.dart` so that the
/// entire app reads from a single source of truth.
export 'package:speedview/common/constants.dart';
